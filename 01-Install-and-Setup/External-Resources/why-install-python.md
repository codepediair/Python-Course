---
title: "چرا باید پایتون (Interpreter) را نصب کنیم؟"
chapter: 1
section: "Setup"
tags: [python, interpreter, compiler, setup]
updated: 2025-09-21
description: "دلیل نیاز به نصب مفسر یا کامپایلر برای اجرای کدهای برنامه‌نویسی."
---

# ❓ چرا باید پایتون (Interpreter) را نصب کنیم؟

وقتی شما کدی به زبان پایتون می‌نویسید، کامپیوتر به صورت مستقیم آن را نمی‌فهمد.  
کامپیوتر فقط زبان ماشین (صفر و یک) را می‌فهمد.  
اینجاست که **مفسر (Interpreter) یا کامپایلر (Compiler)** وارد عمل می‌شود.

---

## 1️⃣ زبان‌های برنامه‌نویسی سطح بالا

- زبان‌هایی مثل Python، Java، C++ برای انسان طراحی شده‌اند تا خوانا و ساده باشند.  
- اما پردازنده (CPU) فقط **زبان ماشین** را می‌فهمد.  
- بنابراین نیاز به یک ابزار واسطه داریم که کد ما را به زبان ماشین ترجمه کند.  

---

## 2️⃣ کامپایلر چیست؟

- کامپایلر یک برنامه است که کل کد شما را **یک‌بار ترجمه** می‌کند و یک فایل اجرایی (Machine Code) می‌سازد.  
- مثال: زبان‌های C و C++  
- ویژگی: سرعت اجرای بالا (چون ترجمه فقط یکبار انجام می‌شود).  

---

## 3️⃣ مفسر (Interpreter) چیست؟

- مفسر کد شما را **خط به خط** می‌خواند و اجرا می‌کند.  
- مثال: Python، JavaScript  
- ویژگی: راحت‌تر برای تست و خطایابی (Debugging)، اما سرعت اجرای کد کمی کمتر است.  

---

## 4️⃣ پایتون چگونه کار می‌کند؟

پایتون یک **مفسر (Interpreter)** است.  
یعنی وقتی شما می‌نویسید:

```python
print("Hello, Python!")
```



مفسر پایتون کد را خط به خط می‌خواند و دستور مناسب را به زبان ماشین ترجمه می‌کند تا CPU آن را اجرا کند.

## 5️⃣ چرا باید پایتون را نصب کنیم؟

- چون پایتون یک زبان مفسری است و بدون مفسر نمی‌توانیم کدی اجرا کنیم.
- نصب پایتون یعنی نصب Python Interpreter روی سیستم.
- این مفسر وظیفه دارد کدی که ما می‌نویسیم را به زبان ماشین تبدیل کند.

### 📌 جمع‌بندی

- کامپیوتر فقط صفر و یک می‌فهمد.
- زبان‌های سطح بالا (مثل پایتون) نیاز به ترجمه دارند.
- این ترجمه را کامپایلر یا مفسر انجام می‌دهد.
- پایتون یک زبان مفسری است، بنابراین نصب پایتون یعنی نصب Interpreter برای اجرای کدها.