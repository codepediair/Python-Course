---
title: "نصب و راه‌اندازی پایتون"
chapter: 1
section: "Setup"
tags: [python, setup, install, environment]
updated: 2025-09-21
description: "در این بخش با نصب پایتون و آماده‌سازی محیط برنامه‌نویسی آشنا می‌شویم."
---

# ⚙️ نصب و راه‌اندازی پایتون

برای شروع یادگیری پایتون باید ابتدا آن را روی [سیستم خود نصب کنیم.](External-Resources/why-install-python.md)  

---

## 1️⃣ بررسی نصب بودن پایتون

در بعضی سیستم‌ها (مثل [لینوکس](External-Resources/linux-introduction.md) و مک) پایتون از قبل نصب است.  
برای بررسی، در ترمینال دستور زیر را وارد کنید:

```bash
python --version
```


اگر عددی مثل `Python 3.12.3` دیدید، یعنی پایتون نصب است.

---

## 2️⃣ دانلود و نصب پایتون (ویندوز)

1. وارد سایت رسمی شوید: [https://www.python.org/downloads/](https://www.python.org/downloads/)
2. آخرین نسخه پایدار (مثلاً Python 3.12) را دانلود کنید.
3. در مراحل نصب حتماً گزینه ✅ **Add Python to PATH** را فعال کنید.
4. بعد از نصب، در Command Prompt تایپ کنید:

```
python --version
```

---

## 3️⃣ نصب در لینوکس / مک

- **لینوکس (Ubuntu/Debian):**

```bash
sudo apt update
sudo apt install python3
```


## 4️⃣ محیط تعاملی (Python REPL)

بعد از نصب می‌توانید با دستور python یا python3 وارد محیط تعاملی شوید:

```
python
```

سپس می‌توانید دستورات ساده مثل جمع و تفریق را اجرا کنید:

``` python
>>> 2 + 3
5
```


5️⃣ ویرایشگرها و IDEهای پیشنهادی

برای نوشتن برنامه‌های بزرگ‌تر بهتر است از یک محیط توسعه استفاده کنید:

    VS Code → سبک و محبوب (پلاگین Python دارد).

    PyCharm → حرفه‌ای و مخصوص پایتون.

    Jupyter Notebook → مناسب برای داده‌کاوی و آموزش.

    حتی Notepad++ یا خود Notepad هم می‌تواند برای کدهای ساده استفاده شود.


## 📌 جمع‌بندی

- پایتون باید روی سیستم نصب شود (ویندوز/لینوکس/مک).
- نصب بودن را با دستور `python --version` بررسی کنید.
- می‌توانید از محیط تعاملی یا IDEها برای نوشتن کد استفاده کنید.