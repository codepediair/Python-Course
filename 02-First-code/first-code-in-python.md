---
title: "اولین کد و پنج عمل اصلی برنامه‌نویسی"
chapter: 2
section: "Basics"
tags: [python, basics, input, output, process, condition, loop]
updated: 2025-09-21
description: "در این بخش اولین کد پایتون را می‌نویسیم و با پنج عمل اصلی برنامه‌نویسی آشنا می‌شویم."
---

# ✨ اولین کد در پایتون

اولین کدی که معمولاً می‌نویسیم چاپ یک متن ساده است که در [کد ادیتور](Python-Course/02-First-code/External-Resources/code-editors-and-ide.md) خودمان مینویسیم:

```python
print("Hello, Python!")
```

حالا با [اجرای کد](Python-Course/02-First-code/External-Resources/how-to-run-python-code.md) این خروجی را خواهید دید

```
Hello, Python!
```

# 🧩 پنج عمل اصلی در برنامه‌نویسی

تقریباً تمام برنامه‌های کامپیوتری را می‌توان در قالب این ۵ عمل توضیح داد:

---

## 1️⃣ ورودی (Input)
گرفتن داده از کاربر یا سیستم.
مثال در پایتون :
```python
name = input("what is your name : ")
```

## 2️⃣ خروجی (Output)
نمایش یا ارسال نتیجه به کاربر یا سیستم.
مثال :
```python
print("hello ", name)
```

## 3️⃣ پردازش (Process)
انجام محاسبات یا تغییر داده‌ها.
مثال :
```python
a = 5
b = 3
c = a + b
print("Result: ", c)
```
## 4️⃣ شرط (Condition)
اجرای تصمیم‌ها بر اساس شرایط
مثال :
```python
age = int(input("سن خود را وارد کنید: "))
if age >= 18:
    print("شما بزرگسال هستید.")
else:
    print("شما کودک هستید.")
```

## 5️⃣ حلقه (Loop)
تکرار عملیات تا زمانی که شرط برقرار باشد.
مثال : 
```python
for i in range(5):
    print("عدد:", i)
```

# 📌 جمع‌بندی

- اولین کد معمولاً با `print()` شروع می‌شود.
- همه برنامه‌ها را می‌توان در ۵ عمل خلاصه کرد:  
    **ورودی، خروجی، پردازش، شرط، حلقه**.
- با ترکیب این ۵ عمل می‌توان هر مسئله‌ای را حل کرد.